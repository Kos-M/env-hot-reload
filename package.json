{
  "name": "env-hot-reload",
  "version": "1.0.1",
  "description": "A small wrapper for watch changes in .env files , changes in  .env auto parsed in process.env",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["dotenv", "envreload", "reload env", "hotreload .env","populate .env live", "update .env without restart"],
  "author": "Kostas Mpoukouvalas",
  "license": "BSD-3-Clause",
  "dependencies": {
    "dotenv": "^10.0.0"
  }
}
